---
id: TKT-015
version: 0.1.0
status: done
source_backlog: TKT-NEW-006-A
completed_at: 2026-05-04
completed_by: Strategic Orchestrator
completed_note: Merged in PR #47 and reviewed in PR #48; closure synchronized by SO after accidental Founder merge audit.
---

# TKT-015: Wire Telegram Adapter To Hermes Gateway Transport

## 1. Scope

Wire the TKT-006 Telegram founder interaction adapter to live Hermes Telegram gateway transport for one trusted founder and one active project.

This ticket should bind inbound Hermes/Telegram messages into `TelegramEvent` inputs, bind outbound `TelegramSender.send()` calls to the reviewed Hermes Telegram gateway path, and preserve the repository governance boundary defined by `HERMES-RUNTIME-CONTRACT.md`.

## 2. Non-scope

- Do not implement GitHub REST or `git` executor binding; TKT-NEW-008-D remains the follow-up for real GitHub HTTP/subprocess execution.
- Do not implement a full Telegram-to-PR trial; TKT-011 remains the end-to-end trial ticket after live Telegram and GitHub runtime bindings are ready.
- Do not change Telegram command semantics beyond the TKT-006 adapter contract unless a narrowly scoped bug is found and documented in the Execution Log.
- Do not implement OpenClaw, a web dashboard, autonomous merges, live VPS deployment, or marketplace/community skills.
- Do not commit `.env`, `config.yaml` containing secrets, Telegram bot tokens, raw private chat IDs, raw private user IDs, PATs, API keys, or VPS credentials.

## 3. Required Context

- `AGENTS.md`
- `CONTRIBUTING.md`
- `docs/architecture/ARCH-001.md`
- `docs/architecture/HERMES-RUNTIME-CONTRACT.md`
- `docs/architecture/HERMES-SKILL-ALLOWLIST.md`
- `docs/architecture/OPERATIONAL-STATE-STORE.md`
- `docs/architecture/adr/ADR-001-platform-foundation.md`
- `docs/architecture/adr/ADR-002-repository-state.md`
- `docs/architecture/adr/ADR-003-plugin-supply-chain.md`
- `docs/tickets/TKT-006.md`
- `docs/tickets/TKT-008.md`
- `docs/tickets/TKT-011.md`
- `docs/tickets/TKT-014.md`
- `docs/backlog/TKT-NEW-006-A.md`
- `docs/backlog/TKT-NEW-008-D.md`
- `docs/reviews/RV-CODE-018.md`
- `docs/reviews/RV-CODE-008.md`

## 4. Acceptance Criteria

- [ ] Inbound Hermes Telegram gateway messages are converted into TKT-006 `TelegramEvent` instances without persisting raw private chat IDs or raw private user IDs in repository artifacts, committed fixtures, logs, or errors.
- [ ] The inbound transport enforces the reviewed founder allowlist or DM pairing requirement before adapter handling; allow-all modes remain disabled.
- [ ] Outbound `TelegramSender.send()` calls are delivered through the reviewed Hermes Telegram gateway path and preserve Russian founder-facing text from the TKT-006 adapter.
- [ ] Runtime configuration validation enforces TKT-012 / `HERMES-SKILL-ALLOWLIST.md` constraints: `TELEGRAM_BOT_TOKEN` may be used only from approved runtime secret storage, founder allowlist or DM pairing is configured, `GATEWAY_ALLOW_ALL_USERS` and `TELEGRAM_ALLOW_ALL_USERS` remain unset/false, polling is preferred for v0.1, and webhook mode requires `TELEGRAM_WEBHOOK_SECRET`.
- [ ] Manual or automated smoke coverage demonstrates sanitized `/new_project`, `/status`, `/decisions`, `/pause`, `/resume`, free-form classification, specialist-question delivery, founder-answer capture, and progress-report delivery through the transport boundary.
- [ ] Smoke-test documentation records commands, sanitized chat/user references, and outcomes without token values, raw private chat IDs, raw private user IDs, or private runtime config.
- [ ] The implementation does not enable Hermes bundled GitHub credential-bearing skills, marketplace skills, project-local plugins, OpenClaw plugins, autonomous merge, or live deployment.
- [ ] Existing TKT-006 adapter behavior remains covered by unit tests, and new transport-boundary tests use mocked Hermes/Telegram gateway inputs and send calls where live credentials are unavailable.
- [ ] `python scripts/validate_docs.py` passes.
- [ ] `python -m unittest discover -s tests -p "test_*.py" -v` passes.

## 5. Allowed Files

- Runtime source/config files explicitly needed to bind the TKT-006 adapter to Hermes Telegram gateway transport.
- `tests/`
- Documentation for sanitized local/manual Telegram smoke-test steps if the implementation needs a non-secret runtime note, limited to docs under the Executor write zone approved by the Ticket Orchestrator.
- `docs/tickets/TKT-015.md` Section 10 Execution Log only.

## 6. Test/Validation Requirements

- Run `python scripts/validate_docs.py`.
- Run `python -m unittest discover -s tests -p "test_*.py" -v`.
- Add tests with mocked Hermes/Telegram gateway inbound payloads and mocked outbound sender behavior.
- Test rejection of unsafe runtime configuration: missing allowlist/pairing, allow-all flags enabled, webhook mode without `TELEGRAM_WEBHOOK_SECRET`, and any path that would expose tokens or raw private identifiers.
- If a live Telegram smoke test is run, document only sanitized evidence: command names, sanitized chat/user keys, high-level outcome, and whether polling or webhook mode was used. Do not print tokens, raw chat IDs, raw user IDs, or private runtime config.

## 7. PR Requirements

- Link this ticket.
- Include validation output for docs validation and unit tests.
- State whether a live Telegram smoke test was run; if not, explain the credential/environment limitation.
- Document configured environment variable names without values.
- Include risk notes for any Hermes gateway API mismatch, manual setup step, or remaining transport limitation.
- State explicitly that TKT-NEW-008-D / live GitHub HTTP and `git` executor binding is out of scope for this ticket.

## 8. Risks

- Hermes gateway APIs or message payload shape may differ from current assumptions, requiring a small adapter shim or a documented follow-up.
- Telegram transport logs can accidentally expose bot tokens, raw chat IDs, or raw user IDs if sanitization is incomplete.
- Webhook mode introduces public endpoint exposure and secret-handling risk; polling is preferred for v0.1 unless webhook mode is explicitly required and protected by `TELEGRAM_WEBHOOK_SECRET`.
- TKT-006 currently uses in-memory progress scheduling; this ticket may deliver progress messages through live transport but does not persist scheduling through SQLite.
- TKT-006's filesystem artifact writer remains a separate concern; this ticket should not broaden into durable file-path generation unless needed for transport wiring.
- End-to-end Telegram-to-PR behavior still depends on GitHub runtime executor binding from TKT-NEW-008-D and the later TKT-011 trial.

## 9. Dependencies

- TKT-006 is done and provides the Telegram founder interaction logic layer.
- TKT-012 is done; production `TELEGRAM_BOT_TOKEN` use is allowed only under `HERMES-SKILL-ALLOWLIST.md` constraints.
- TKT-007 and TKT-013 are done; SQLite operational state exists, but this ticket is not required to solve progress-schedule persistence.
- TKT-008 is done and provides high-level GitHub PR integration logic, but live GitHub execution is not required for this Telegram gateway transport ticket.
- TKT-014 is done and provides the project-specific GitHub workflow capability; it is context only for avoiding Hermes bundled GitHub skills in this ticket.
- TKT-NEW-008-D remains backlog/follow-up for binding GitHub REST and `git` executors to real runtime execution.
- TKT-011 remains draft and should wait for this live Telegram transport wiring plus GitHub runtime binding if the trial is meant to be fully live.
- Executor default model is DeepSeek V4 Pro for comparison against prior GLM 5.1 results; no specialist model is required by this ticket.

## 10. Execution Log

### Iter-1: DeepSeek V4 Pro (2026-05-04)

- **Branch**: `tkt-015/telegram-gateway-transport`
- **Base SHA**: `e0cb258c883c7b91d10b54ff78f64d4cffda4ff9`

#### Files Changed
- `src/developer_assistant/hermes_telegram_transport.py` (new, 214 lines)
- `tests/test_hermes_telegram_transport.py` (new, 838 lines, 81 test methods)
- `docs/tickets/TKT-015.md` (Section 10 only)

#### Implementation Summary
Created `hermes_telegram_transport.py` providing:
- `HermesGatewayPayload` — sanitized inbound payload from Hermes Telegram gateway with validation that rejects raw numeric chat/user IDs
- `TransportConfig` — configuration dataclass with `validate()` enforcing: no allow-all modes, allowlist/DM pairing required, bot token configured, polling preferred, webhook only with secret, and mutual exclusion of polling+webhook
- `HermesTelegramSender` — implements `TelegramSender` protocol, delivers through an injectable `OutboundCallback` transport boundary abstraction
- `HermesTelegramTransport` — wires gateway → TelegramEvent → adapter with authorization at transport boundary, plus `deliver()` and `create_sender()` for outbound
- `sanitize_gateway_payload()` — helper to construct sanitized payloads from raw gateway data
- `validate_transport_config_env()` — standalone env-based validation function

#### Validation Results
- `python scripts/validate_docs.py` — **passed**
- `python -m unittest discover -s tests -p "test_*.py" -v` — **387 tests pass (1 skipped)**

#### Test Coverage Summary (81 transport tests)
- Gateway payload validation: 10 tests (valid, missing fields, raw numeric ID rejection)
- Transport config validation: 12 tests (allow-all rejection, missing token, empty allowlist, DM pairing, webhook without secret, mutual exclusion, no token values stored)
- Transport authorization: 7 tests (allowlist, DM pairing, allow-all disabled)
- Inbound transport wiring: 6 tests (authorization, rejection, sanitized keys)
- Smoke coverage per command: `/new_project` (2), `/status` (2), `/decisions` (2), `/pause` (2), `/resume` (2)
- Free-form classification: 5 tests (intake, approval, rejection, clarification, general question)
- Specialist-question delivery: 2 tests
- Founder-answer capture: 2 tests
- Progress-report delivery: 2 tests
- Outbound sender: 5 tests
- Token/private-ID leak prevention: 6 tests
- `validate_transport_config_env`: 10 tests
- Existing adapter behavior preserved: 5 tests

#### Live Telegram Smoke-Test Status
**Not run.** Credentials and live Hermes Telegram gateway environment are not available during this Executor session. The transport boundary is tested with mocked gateway payloads and mocked outbound callbacks.

#### Security Confirmation
- No Telegram bot tokens committed
- No raw private chat IDs or raw private user IDs in committed code, fixtures, logs, or error messages
- No `config.yaml` containing secrets
- `GATEWAY_ALLOW_ALL_USERS` and `TELEGRAM_ALLOW_ALL_USERS` remain unset/false
- No Hermes bundled GitHub credential-bearing skills enabled
- No marketplace skills, project-local plugins, or OpenClaw plugins enabled
- No autonomous merge or live deployment enabled

#### Known Limitations / Follow-ups
- Live Telegram smoke test deferred until Hermes deployment with valid credentials
- Progress scheduling remains in-memory only (not persisted through SQLite); follow-up ticket may be needed
- TKT-NEW-008-D (live GitHub REST and `git` executor binding) remains backlog
- TKT-011 (full Telegram-to-PR trial) remains draft and out of scope
- Hermes gateway API payload shape may differ from the mocked format used here; a small adapter shim may be needed when deployed

### Closure — 2026-05-04

- **Executor PR**: #47 merged at `cfc1fdf` from final head `65ea130af3d4d548786b694b3cf2160e490f3ce6`.
- **Reviewer PR**: #48 merged at `e8f1d2b` from final head `8455057b9760d0ef5655e1413a295665cf12a33b`.
- **Reviewer artifact**: `docs/reviews/RV-CODE-019.md`, verdict `pass`.
- **SO ratification**: Passed after confirming PR-Agent and Docs CI were green on both PRs, the merged files matched TKT-015 scope, Reviewer verdict was `pass`, and merged `main` validation passed with 387 tests OK and 1 skipped.
- **Process note**: Founder merged #47 and #48 before the final SO merge-order message. SO audited the already-merged state and found no emergency reason to revert.
- **Accepted residual risks / follow-ups**:
  - Live Telegram smoke test remains required before `TKT-011` because credentials and live Hermes Telegram gateway environment were unavailable during implementation.
  - Hermes gateway API payload shape may require a small deployment shim if it differs from the mocked contract.
  - Progress scheduling remains in-memory only; durable scheduling through SQLite may need a follow-up ticket before production runtime use.
  - `TKT-NEW-008-D` remains the next likely prerequisite for a fully live Telegram-to-PR trial.
