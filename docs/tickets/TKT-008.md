---
id: TKT-008
version: 0.3.0
status: ready
---

# TKT-008: Implement GitHub Repository and PR Integration

## 1. Scope

Implement the GitHub integration required for Hermes-orchestrated project creation, branch/PR workflow, CI observation, and review gate tracking by wiring the project-specific GitHub workflow capability delivered by TKT-014 into the runtime/orchestration path.

TKT-012 rejected Hermes bundled GitHub credential-bearing skills (`github-pr-workflow`, `github-issues`, `github-auth`) for production `GITHUB_TOKEN` / `GH_TOKEN` use. TKT-014 is complete and provides the reviewed project-specific REST API + constrained `git` workflow capability that this ticket must use.

## 2. Non-scope

- Do not implement autonomous merges.
- Do not store GitHub tokens in repository files.
- Do not implement deployment to VPS.
- Do not bypass ticket, CI, or Reviewer gates.
- Do not use Hermes bundled `github-pr-workflow`, `github-issues`, or `github-auth` with production GitHub credentials.
- Do not reimplement the low-level GitHub REST request and constrained `git` command builders already delivered by TKT-014 unless a narrowly scoped gap is discovered and documented in the Execution Log.

## 3. Required Context

- `AGENTS.md`
- `CONTRIBUTING.md`
- `docs/architecture/ARCH-001.md`
- `docs/architecture/HERMES-RUNTIME-CONTRACT.md`
- `docs/architecture/HERMES-SKILL-ALLOWLIST.md`
- `docs/architecture/adr/ADR-001-platform-foundation.md`
- `docs/architecture/adr/ADR-003-plugin-supply-chain.md`
- `docs/architecture/OPERATIONAL-STATE-STORE.md`
- `docs/reviews/RV-CODE-009.md`
- `docs/reviews/RV-CODE-014.md`
- `docs/reviews/RV-CODE-018.md`
- `docs/tickets/TKT-006.md`
- `docs/tickets/TKT-004.md`
- `docs/tickets/TKT-005.md`
- `docs/tickets/TKT-009.md`
- `docs/tickets/TKT-012.md`
- `docs/tickets/TKT-014.md`

## 4. Acceptance Criteria

- [ ] The integration can create or register a GitHub repository for a project.
- [ ] The integration can create branches and open PRs linked to one ticket.
- [ ] The integration can read CI/check status for a PR.
- [ ] The integration can attach or reference Reviewer artifacts under `docs/reviews/`.
- [ ] The integration requires founder acknowledgement before merge in v0.1.
- [ ] GitHub credentials are scoped and supplied through the TKT-014-approved `PROJECT_GITHUB_PAT` runtime secret, not committed files, `GITHUB_TOKEN` / `GH_TOKEN` fallback, `~/.git-credentials`, token-bearing remotes, or CLI arguments.
- [ ] The integration uses the project-specific GitHub workflow capability from TKT-014 as the primary GitHub operation path and does not enable Hermes bundled GitHub skills rejected by TKT-012.
- [ ] The integration composes GitHub PR state with the TKT-006 Telegram founder interaction logic so founder-facing status/progress/approval messages can reference repository, PR, CI, and review-gate state without treating Telegram chat history as authoritative.
- [ ] `python scripts/validate_docs.py` passes.

## 5. Allowed Files

- Runtime source/config files explicitly established by the approved implementation plan.
- `.github/` only for templates or workflows directly required by this ticket.
- `tests/`
- `docs/tickets/TKT-008.md` Execution Log only

## 6. Test/Validation Requirements

- Run `python scripts/validate_docs.py`.
- Add or run tests with mocked GitHub API behavior where feasible.
- If a live GitHub smoke test is run, document repository/PR URLs without printing secrets.

## 7. PR Requirements

- Link this ticket.
- Include validation and test output.
- Document required GitHub token scopes.
- Document any live-test limitations.

## 8. Risks

- Overbroad GitHub tokens can expose all assistant-owned repositories.
- PR automation can bypass governance if ticket links and review gates are not enforced.
- Live API tests may be flaky or unavailable in local environments.
- Hermes bundled GitHub skills failed TKT-012 source review for production credential-bearing use; enabling them would violate ADR-003 controls and v0.1 founder-acknowledgement gates.
- The project-specific GitHub workflow from TKT-014 is reviewed and tested, but TKT-008 can still introduce policy bypasses if it does not use `load_credential()` as the primary credential entry point, does not preserve token redaction, or circumvents constrained command builders.
- TKT-006 currently provides a logic-layer Telegram adapter, not live Hermes/Telegram transport; TKT-008 should integrate with that logic contract without assuming live gateway wiring is already complete.

## 9. Dependencies

- TKT-002 and TKT-004 should be complete.
- TKT-005 must be done and must define how Hermes runtime work references repository governance artifacts.
- TKT-009 must be done before enabling credential-bearing GitHub automation.
- TKT-012 is done and blocks Hermes bundled GitHub credential-bearing skills from production use.
- TKT-014 is done and provides the reviewed project-specific REST API + constrained `git` workflow capability used by this ticket.
- TKT-006 is done and provides the Telegram founder interaction logic layer used for founder-facing status, progress, and approval messages.
- User-approved baseline: GitHub credentials use a fine-grained PAT first, with GitHub App deferred until later.

## 10. Execution Log

### 2026-05-04

- **Branch**: `tkt-008/github-pr-integration`
- **Base SHA**: `8dfed1be02c8f5c8f1f11232f74255d984de75e7`
- **Files changed**:
  - `src/developer_assistant/github_pr_integration.py` (new) — High-level GitHub PR integration layer wiring TKT-014 capability into runtime/orchestration path
  - `tests/test_github_pr_integration.py` (new) — 75 unit tests covering all TKT-008 acceptance criteria
  - `docs/tickets/TKT-008.md` (Section 10 only) — This Execution Log
- **Summary of implementation**:
  - `GitHubPRIntegration` class: high-level orchestration layer that composes TKT-014's `github_workflow.py` REST request construction and constrained git command construction into the runtime/orchestration path
  - `ReviewGateState` dataclass: tracks review-gate state linked to PR/ticket, validates artifact paths start with `docs/reviews/` and end with `.md`
  - `PRCheckState` dataclass: CI/check status for a PR
  - `ProjectGitHubState` dataclass: composite GitHub state for a project, renders Russian-language Telegram status and progress text with repo, PR, CI, review-gate, ticket, and merge-gate state
  - `compose_github_aware_progress_report()`: composes GitHub state with TKT-006 Telegram progress report format
  - All network calls injectable through `RESTExecutor` and `GitExecutor` protocols; no live GitHub access required for tests
  - Credential entry point: `load_credential()` from TKT-014 exclusively via `PROJECT_GITHUB_PAT`
  - Founder acknowledgement merge gate enforced at integration layer in addition to TKT-014's `build_merge_command()` gate
  - Token values redacted via `_redact_url()` (URL credential patterns) and `redact_token()` (GitHub token patterns)
  - Does not import, enable, or use Hermes bundled `github-pr-workflow`, `github-issues`, or `github-auth`
- **Validation commands and results**:
  - `python scripts/validate_docs.py`: Docs validation passed
  - `python -m unittest discover -s tests -p "test_*.py" -v`: 293 tests OK, 1 skipped (218 pre-existing + 75 new TKT-008)
- **Test count**: 75 TKT-008 tests covering:
  - Credential use through `load_credential()` (6 tests)
  - Repository create/register (8 tests)
  - Branch creation and PR open linked to ticket (8 tests)
  - Check-status read / PR metadata read (7 tests)
  - Reviewer artifact reference/validation (11 tests)
  - Founder acknowledgement merge gate (8 tests)
  - Telegram status/progress composition (11 tests)
  - Secret hygiene: no token values in URLs, errors, progress text (8 tests)
  - Hermes bundled skills blocked / project-specific workflow used (2 tests)
  - Dataclass and update behavior (8 tests)
- **Acceptance criteria status**:
  - Create/register GitHub repository: PASS
  - Create branches and open PRs linked to one ticket: PASS
  - Read CI/check status for a PR: PASS
  - Attach/reference Reviewer artifacts under docs/reviews/: PASS
  - Founder acknowledgement before merge in v0.1: PASS
  - Credentials scoped through PROJECT_GITHUB_PAT, not prohibited sources: PASS
  - Uses TKT-014 project-specific workflow, not Hermes bundled skills: PASS
  - Composes GitHub PR state with TKT-006 Telegram logic: PASS
  - `python scripts/validate_docs.py` passes: PASS
  - Relevant unit tests pass: PASS
- **Credential-source/security confirmation**:
  - Credentials accepted ONLY from `PROJECT_GITHUB_PAT` via `load_credential()`
  - `GITHUB_TOKEN`, `GH_TOKEN`, `~/.git-credentials`, token-bearing remotes, committed config, and CLI arguments are NOT accepted as fallback
  - No Hermes bundled GitHub skills are imported or used
  - No token values, PAT-like values, .env files, credential files, or token-bearing remotes in committed code or test fixtures
  - Test fixtures use `FAKE_TEST_TOKEN_NOT_REAL_1234567890` and `FAKE_PAT_NOT_REAL_AAA...` strings that do not match real GitHub token prefixes
- **Live GitHub smoke-test status**: Not run. No live GitHub credentials available in this runtime environment. All tests use mocked REST/git executors.

### Iteration 2 — 2026-05-04

- **Finding PA-1**: PR-Agent security-class finding — `read_pr_metadata` docstring promises redaction but returned dict was raw. If a malformed API response contained token-bearing strings, a caller relying on the docstring could leak credentials.
- **Fix applied**:
  - Added `_redact_value(value)`: recursive redaction helper that applies `_redact_url()` and `redact_token()` to string values, recurses into dict/list/tuple containers, and preserves non-string scalars (int, bool, None) unchanged. Returns a sanitized copy; does not mutate the raw response object.
  - Changed `read_pr_metadata` to return `_redact_value(result)` instead of the raw `result` dict. The `state` field is read from the raw dict before redaction so that `active_pr_state` update uses the unredacted value.
- **Files changed**:
  - `src/developer_assistant/github_pr_integration.py` — Added `_redact_value()`, changed `read_pr_metadata` return to use it
  - `tests/test_github_pr_integration.py` — Added `TestReadPRMetadataRedaction` class with 13 tests; updated module docstring and imports
  - `docs/tickets/TKT-008.md` (Section 10 only) — This Iteration 2 entry
- **Validation commands and results**:
  - `python scripts/validate_docs.py`: Docs validation passed
  - `python -m unittest discover -s tests -p "test_*.py" -v`: 306 tests OK, 1 skipped (218 pre-existing + 75 iter-1 TKT-008 + 13 new iter-2 redaction tests)
- **New test count**: 13 redaction tests covering:
  - Redacts `ghp_...` token strings in returned dict fields (1)
  - Redacts `github_pat_...` token strings in returned dict fields (1)
  - Redacts token-bearing URLs such as `https://ghp_...@github.com/owner/repo` (1)
  - Redacts nested dict fields (1)
  - Redacts nested list fields (1)
  - Preserves non-string metadata values (int, bool, None) unchanged (1)
  - Does not mutate the raw response object (1)
  - `_redact_value` unit: ghp token (1), URL (1), nested dict (1), nested list (1), tuple (1), int/bool/None (1)
- **No secrets committed**: Test fixtures use `ghp_ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789` and `github_pat_1A2B3C4D5E6F7G8H9I0J1K2L3M4N5O6P7Q8R9S0` which are non-real patterns for redaction verification only.
- **Credential policy unchanged**: `PROJECT_GITHUB_PAT` via `load_credential()` remains the sole credential source. No new credential paths introduced.
