---
id: TKT-030-FIX-001
version: 0.1.0
status: ready
origin: TKT-030 harness detection
severity: low
---

# TKT-030-FIX-001: Reconcile omniroute.service unit name in RECOVERY-PLAYBOOK.md

## Problem

RECOVERY-PLAYBOOK.md references `devassist-omniroute.service` in 4 locations (lines 168, 203, 211, 212), but `SELF-DEPLOYMENT-CONTRACT.md` §5.3 defines the unit as `omniroute.service` (no `devassist-` prefix). The TKT-030 harness detects this as a KNOWN_PLAYBOOK_INCONSISTENCY failure.

## Fix

Replace all 4 occurrences of `devassist-omniroute.service` with `omniroute.service` in `docs/operations/RECOVERY-PLAYBOOK.md`.

Remove the `devassist-omniroute.service` entry from `KNOWN_PLAYBOOK_INCONSISTENCIES` in `tests/fixtures/recovery_playbook/command_validators.py`.

Verify: `python -m unittest tests.test_recovery_playbook_invariants -v` passes with 0 failures.

## Allowed Files

- `docs/operations/RECOVERY-PLAYBOOK.md` (line edits only: 4 string replacements)
- `tests/fixtures/recovery_playbook/command_validators.py` (remove KNOWN_PLAYBOOK_INCONSISTENCIES entry)
- `docs/tickets/TKT-030.md` §10 only (execution log append)

## Notes

This is a clerical doc fix, not a code change. Architect write-zone (playbook) + Executor write-zone (test fixture). No new functionality.
